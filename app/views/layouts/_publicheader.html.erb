<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="nav-container">
    <div class="navbar-header">
      <button class="navbar-toggle" data-target="#bs-example-navbar-collapse-1" data-toggle="collapse" type="button">
        <span class="sr-only">Toggle navigation</span> <span class="icon-bar"></span> 
        <span class="icon-bar"></span> 
        <span class="icon-bar"></span>
      </button> 
      <a class="navbar-brand" href="<%= root_path %>" style="padding:0;">
      <img class="header-logo" src="<%= @static_page_info.logo %>"></a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if current_page?(contact_path) %>
          <% categories_for_header.each do |category| %>
            <li>
              <a class="slideup" href="<%= root_path %>#<%= category %>_scroll" id="work_link"><%= category.sub("_Amp_", " & ").sub("_", " ") %></a>
            </li>
            <li class="navLine">|</li>
          <% end %>
            <li>
              <a href="<%= root_path %>#about_scroll" id="about_link">About Us</a>
        <% else %>
          <% categories_for_header.each do |category| %>
            <li>
              <a class="slideup<%= category %>" id="work_link" href="#"><%= category.sub("_Amp_", " & ").sub("_", " ") %></a>
            </li>
            <li class="navLine">|</li>
          <% end %>
          <li>
            <a class="slideupAbout" id="about_link" href="#">About Us</a>
        <% end %>
        </li>
        <li class="navLine">|</li>
        <li>
          <a href="contact" id="contact_link">Contact</a>
        </li>
      </ul>
    </div>
  </div>
</nav>